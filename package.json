{
  "name": "chat-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://elshad98.github.io/chat-app",
  "dependencies": {
    "@atlaskit/css-reset": "^4.0.1",
    "@pusher/chatkit": "^0.7.18",
    "@pusher/chatkit-client": "^1.5.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
